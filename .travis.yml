language: go

services:
  - docker

notifications:
  email:
    on_success: never
    on_failure: true

deploy:
  provider: script
  script: bash hack/docker_push.sh
  on:
    branch: master

script: make
